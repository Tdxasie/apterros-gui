// make class that stores filters
// use case : import Filters from ${path}
// Filters.mean(window, data)

// should count the data entries itself, mqtt should not have any computing to do
// counters and resets should be handled internally